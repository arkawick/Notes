"""Shim Django app standing in for ``somc-django-inlines``.

Provides the ``inlines_tags`` template library. The inline templates themselves
already live in the application repositories, so nothing else is needed.
"""
